# image.tag is bumped automatically by norviq-mcp CI on merge to main.
image:
  tag: "d4ffadf087dd86c39ccb1ed644f20fbb754c4535"

env:
  - name: BACKEND_BASE_URL
    value: http://api:8080
  - name: MCP_PUBLIC_URL
    value: https://dev-mcp.norviq.org
  - name: LISTEN_ADDR
    value: ":8087"

resources:
  requests:
    cpu: 25m
    memory: 32Mi
  limits:
    memory: 96Mi

ingress:
  hosts:
    - dev-mcp.norviq.org
