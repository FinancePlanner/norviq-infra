apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: gotenberg-staging
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/FinancePlanner/norviq-infra.git
    targetRevision: main
    path: charts/app
    helm:
      releaseName: gotenberg
      valueFiles:
        - ../../apps/gotenberg/values-common.yaml
        - ../../apps/gotenberg/values-staging.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: staging
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    retry:
      limit: 5
      backoff:
        duration: 30s
        factor: 2
        maxDuration: 5m
