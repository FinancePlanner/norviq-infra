# Replace with real sealed output — see secrets/README.md.
# MCP_INTROSPECTION_SECRET must match the key added to the staging api-env secret.
#
# kubectl create secret generic mcp-env -n staging --from-env-file=.env.mcp \
#   --dry-run=client -o yaml \
#   | kubeseal --controller-name sealed-secrets-controller \
#              --controller-namespace kube-system -o yaml \
#   > secrets/staging/mcp-env.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: mcp-env
  namespace: staging
spec:
  encryptedData:
    MCP_INTROSPECTION_SECRET: AgB...
  template:
    metadata:
      name: mcp-env
      namespace: staging
