# Replace with real sealed output (see secrets/README.md). Shared by api + mcp.
# printf 'MCP_INTROSPECTION_SECRET=<value>\n' | kubectl create secret generic mcp-introspection \
#   -n staging --from-env-file=/dev/stdin --dry-run=client -o yaml \
#   | kubeseal --controller-name sealed-secrets-controller --controller-namespace kube-system -o yaml \
#   > secrets/staging/mcp-introspection.yaml
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: mcp-introspection
  namespace: staging
spec:
  encryptedData:
    MCP_INTROSPECTION_SECRET: AgB...
  template:
    metadata:
      name: mcp-introspection
      namespace: staging
