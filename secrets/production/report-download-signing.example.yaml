# Replace with real sealed output. Generate a different value per environment:
# openssl rand -hex 32 | kubeseal --raw \
#   --from-file=REPORT_DOWNLOAD_SIGNING_SECRET=/dev/stdin \
#   --name report-download-signing --namespace production --scope strict
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: report-download-signing
  namespace: production
spec:
  encryptedData:
    REPORT_DOWNLOAD_SIGNING_SECRET: AgB...
  template:
    metadata:
      name: report-download-signing
      namespace: production
